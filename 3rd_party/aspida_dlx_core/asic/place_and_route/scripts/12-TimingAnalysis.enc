# Perform STA

setCteReport

setAnalysisMode -setup -async -skew -clockTree -moduleIOCstr -useDetailRC -noTimeBorrowing
extractRC -outfile ASPIDA_top.cap
buildTimingGraph

reportTA -max_paths 100 -outfile ASPIDA_tr.STA

#reportCapViolation -all -outfile CAPVIOLATIONS_tr.ENC
reportTranViolation -all -outfile TRANVIOLATIONS_tr.ENC
reportFanoutViolation -all -outfile FANOUTVIOLATIONS_tr.ENC
